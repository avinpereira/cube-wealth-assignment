server:
  port: 8081
spring:
  thymeleaf:
    cache: false
  security:
    user:
      name: avin
      password: password
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost/event}
    username: "postgres"
    password: "docker"
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: 'create-drop'


logging:
  level:
    com.bankoncube.eventsingestor: INFO
  file:
    name: push-notifications
#  pattern:
#    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%'



